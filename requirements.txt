# --- Coeur applicatif -------------------------------------------------------
openai==1.55.3
litellm==1.52.12
python-dotenv==1.0.1
requests==2.32.3
pyyaml==6.0.2
colorama==0.4.6
chromadb==0.5.0
fastapi==0.111.0
# uvicorn relevé à >=0.31.1 (requis par mcp) ; 0.48.0 reste compatible fastapi 0.111.
uvicorn==0.48.0
pydantic==2.13.4
python-multipart==0.0.29

# --- Support MCP (Model Context Protocol) ----------------------------------
mcp==1.27.2
# starlette/sse-starlette ÉPINGLÉS : la dernière sse-starlette (3.x) exige
# starlette>=0.49 et casse fastapi 0.111 (qui veut starlette<0.38).
starlette==0.37.2
sse-starlette==2.1.3

# --- Exécution distante SSH -------------------------------------------------
paramiko==5.0.0
cryptography>=38.0.4

# --- Génération / traitement d'images --------------------------------------
Pillow==9.4.0

# --- Pièces jointes du chat (extraction de texte PDF) ----------------------
pypdf==6.12.2

# --- Optionnel : transcription de réunions (lourd, tire PyTorch) ------------
# Décommentez si vous utilisez transcribe_and_summarize_meeting :
# openai-whisper==20250625

# --- Automatisation Web (Computer Use RPA) ----------------------------------
playwright>=1.40.0
